// Токен и гист
var github_token   =   '' ;
var gist_id        =   '' ;
var max_length     =   500;


// Отправка и чтение
function send(sd) {
    /* Отправка файла на сервер (Гист)
     * С зашифровкой
    */
}

function read() {
    /* Чтение файла с сервера (Гист)
     * С расшифровкой
    */
    return rd;
}


// Очистка
function check_len() {
    text = read()
    if (text.length() == max_length) {
        send("0")
    }
}


// Нужно добавить функцию для токенов